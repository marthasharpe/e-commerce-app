const DashboardPage = () => {
  //   const { storeId } = useParams<{ storeId: string }>();
  //   const { data: store } = useStore(storeId);

  return (
    <div>
      <h1>Dashboard</h1>
      {/* <h2>{store?.name}</h2> */}
    </div>
  );
};

export default DashboardPage;
